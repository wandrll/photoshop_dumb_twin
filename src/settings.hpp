#ifndef SETTINGS
#define SETTINGS
#include "graphics_provider.hpp"

const int RESOLUTION_WIDTH = 1920;
const int RESOLUTION_HEIGHT = 1080;

const int TOP_BAR_HEIGHT = 25;
const int BORDER_THICKNESS = 2;

const int SLIDER_WIDTH = 10;

const int SLIDER_SIZE = 20;
const Color FONT_COLOR(240, 97,86);

const Color BORDER_COLOR(0, 0, 0);

const Color BUTTON_COLOR(32, 32, 32, 255);
#endif

